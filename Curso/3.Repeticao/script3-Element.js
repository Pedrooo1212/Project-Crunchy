const array = ["Azul", "Preto", "Vermelho"];

for (let index = 0; index < array.length; index++) {
    console.log(array[index]);
    console.log('-------------');
    const element = array[index];
    console.log(element);
    console.log('---------------');
};