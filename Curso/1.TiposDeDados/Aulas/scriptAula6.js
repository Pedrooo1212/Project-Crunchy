const timeA = {
    nome: "Real Madrid",
    pais: "Espanha"
};

const gols = [
    4, 3, 0, 2
];

// TimeA é um OBJETO
// gols é um Array

// Como acessar um objeto?
// Como acessar um Array?


// Para acessar um objeto, usamos . ponto
// Para acessar um array usamos [] colchetes e a sua posição


const soma = gols[0] + gols[1] + gols[2] + gols[3]
console.log(soma);


const nomes = ['Pedro', "Jorge", "Darcy"];
console.log(nomes[0])


const calvos = [true, false, true, false, false];
console.log(calvos[0])